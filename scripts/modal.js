/* export function toModal(tech) {
return `<div class="modal-window">
          <h3 class="modal-title">${tech.title}</h3>
          <p class="description">${tech.description}</p>
          <div class="chek">
            <input type="checkbox" name="chekBtn" id="done" ${tech.done ? 'checked' : ''} />
            <label for="done">Выучил</label>
          </div>
        </div>`
}


         */